---
import Layout from '../layouts/Layout.astro';


const url = `${import.meta.env.PUBLIC_API_URL}/guitarras/`
const res = await fetch(url)
const {data} = await res.json()
console.log(data)

---

<Layout title="Tienda" description="Tienda de instrumentos musicales, cursos, blog de musica, guitarras">
	<main class="contenedor">
		<h1 class="heading">Nuestra Colección</h1>

		<div class="grid">
			{data?.map(guitarra => (
				<h1>{guitarra.attributes.nombre}</h1>
			))}

		</div>


	</main>
</Layout>

<style>
	
</style>